// TODO: Move these to be read from environment variables or file so can be configured on per environment basis
const listApiUrl = "http://localhost:3000"

export const getListApiUrl =  () => {
  return listApiUrl
}